# serverless.yml

falsyExempleServerlessNext:
    component: "@sls-next/serverless-component@1.19.0-alpha.37"
    inputs:
        roleArn: AWS_SERVERLESS_ROLE
        bucketName: "kormelon-front"
        cloudfront:
            distributionId: AWS_CLOUD_FRONT
        name:
            defaultLambda: "kormelon"
            apiLambda: "kormelon-api"
