import folderOpen from '../../public/static/folder-open.png';
import folderClose from '../../public/static/folder-close.png';

// in js
const icons = {
	folderOpen,
	folderClose,
};

// in css
const staticUrls = {
	folderOpen: 'static/folder-open.png',
	folderClose: 'static/folder-close.png',
};

export { icons, staticUrls };
