import { createSlice } from '@reduxjs/toolkit';

interface InitialState {}

const initialState: InitialState = {};

const asider = createSlice({
  initialState,
  name: 'asider',
  reducers: {},
});

export const {} = asider.actions;

export default asider.reducer;
